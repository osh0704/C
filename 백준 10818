#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
int main()
{
	int T, iNum, min = 1000002, max = -1000010;
	scanf("%d", &T);
	for (int i = 0; i < T; i++) {
		scanf("%d", &iNum);
		if (iNum < min) min = iNum;
		if (iNum > max) max = iNum;
	}
	printf("%d %d", min, max);
}
