#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
int main()
{
	int sum = 0, num, iNum; 
	scanf("%d", &num);
	for (int i = 0; i < num; i++) {
		scanf("%1d", &iNum);
		sum += iNum;
	}
	printf("%d", sum);
}
