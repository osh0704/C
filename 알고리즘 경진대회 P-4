#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
int main()
{
	int n, S, M, I, sum[1000];
	scanf("%d", &n);
	for (int i = 0; i < n; i++) { 
		scanf("%d %d %d", &S, &M, &I);
		sum[i] = (S + M + I) / 2;
	}
	for (int i = 0; i < n; i++) {
		printf("%d\n", sum[i]);
	}
	return 0;
}
